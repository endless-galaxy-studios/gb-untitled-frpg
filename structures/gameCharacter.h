typedef struct gameCharacter {
    UBYTE spriteID[4];  
    UINT8 x;            // Character's x position  
    UINT8 y;            // Character's y position
    UINT8 width;        // Character's width according to pixel size
    UINT8 height;       // Character's height according to pixel size
} gameCharacter;